# M7开发计划：实时模拟盘接入

## 1. 开发目标

实现一个**持续运行的实时模拟盘系统**，支持策略实时运行、交易执行、数据持久化和系统重启恢复，最终通过Web监控面板实时展示模拟交易结果。

## 2. 核心需求

1. **连接核心交易引擎**：将Web应用与实际的模拟交易引擎连接
2. **实现实时数据更新**：添加定时任务或WebSocket连接，实时更新数据
3. **数据持久化**：将交易记录、持仓、资金等数据保存到数据库
4. **重启恢复机制**：实现系统重启后从数据库恢复状态的功能
5. **后台运行**：将交易引擎作为后台服务运行，与Web应用分离

## 3. 技术架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                               Web应用层                                 │
├───────────┬───────────┬───────────────────┬─────────────────────────────┤
│ 监控面板  │ 策略管理  │    数据可视化     │  REST API 服务              │
└───────────┴───────────┴───────────────────┴─────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              业务逻辑层                                 │
├───────────┬───────────┬───────────────────┬─────────────────────────────┤
│ 事件引擎  │ 策略引擎  │    执行引擎       │  投资组合管理               │
└───────────┴───────────┴───────────────────┴─────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              数据持久层                                 │
├───────────┬───────────┬───────────────────┬─────────────────────────────┤
│ SQLiteDB  │ 数据模型  │    持久化管理器   │  重启恢复机制    |Redis           │
└───────────┴───────────┴───────────────────┴─────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              服务层                                     │
├───────────┬───────────┬─────────────────────────────────────────────────┤
│ 后台服务  │ 定时任务  │                  系统监控                       │
└───────────┴───────────┴─────────────────────────────────────────────────┘
```

## 4. 详细开发计划

### 4.1 阶段一：数据持久化实现

**目标**：实现交易数据的持久化存储，包括订单、成交记录、持仓和资金数据

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.1.1 设计数据模型 | 开发组 | 1天 | 设计数据库表结构，包括：<br>- orders: 订单表<br>- fills: 成交记录表<br>- positions: 持仓表<br>- portfolio: 投资组合表<br>- trades: 交易记录表<br>- system_status: 系统状态表 | 提供详细的数据模型设计文档 |
| 4.1.2 实现数据库连接管理 | 开发组 | 1天 | 创建数据库连接池，实现自动重连和错误处理 | 数据库连接稳定，支持多线程访问 |
| 4.1.3 实现TransactionManager | 开发组 | 2天 | 实现交易管理器，负责：<br>- 订单的持久化<br>- 成交记录的持久化<br>- 持仓数据的持久化<br>- 资金数据的持久化 | 所有交易数据正确保存到数据库 |
| 4.1.4 集成到执行引擎 | 开发组 | 1天 | 将TransactionManager集成到ExecutionEngine，实现交易数据的实时持久化 | 执行引擎提交订单时自动保存到数据库 |

### 4.2 阶段二：重启恢复机制实现

**目标**：实现系统重启后从数据库恢复状态的功能

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.2.1 实现状态恢复管理器 | 开发组 | 2天 | 创建StateManager，负责：<br>- 从数据库加载订单<br>- 从数据库加载持仓<br>- 从数据库加载资金数据<br>- 恢复系统状态 | 系统重启后能正确恢复所有状态 |
| 4.2.2 集成到系统启动流程 | 开发组 | 1天 | 在系统启动时调用StateManager恢复状态 | 系统启动时自动加载历史数据 |
| 4.2.3 实现状态检查机制 | 开发组 | 1天 | 实现状态一致性检查，确保恢复的数据有效 | 恢复后数据一致，无冲突 |

### 4.3 阶段三：后台服务实现

**目标**：将交易引擎作为后台服务运行，与Web应用分离

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.3.1 设计后台服务架构 | 开发组 | 1天 | 设计后台服务架构，包括：<br>- 服务启动和停止<br>- 信号处理<br>- 日志管理<br>- 错误恢复 | 提供后台服务设计文档 |
| 4.3.2 实现后台服务 | 开发组 | 2天 | 使用Python实现后台服务，支持：<br>- 后台运行<br>- 服务管理（start/stop/restart/status）<br>- 日志轮转 | 服务能稳定后台运行 |
| 4.3.3 实现进程管理 | 开发组 | 1天 | 使用进程管理工具（如supervisor）管理后台服务 | 服务能自动重启，支持状态监控 |
| 4.3.4 实现服务监控 | 开发组 | 1天 | 实现服务健康检查和监控 | 能实时监控服务运行状态 |

### 4.4 阶段四：实时数据更新实现

**目标**：实现Web应用与交易引擎的实时数据交互

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.4.1 实现REST API服务 | 开发组 | 2天 | 创建REST API服务，提供：<br>- 获取实时行情<br>- 获取持仓数据<br>- 获取交易记录<br>- 获取绩效指标<br>- 管理策略 | API响应时间<500ms |
| 4.4.2 实现WebSocket连接 | 开发组 | 2天 | 实现WebSocket服务，支持：<br>- 实时推送交易信号<br>- 实时推送成交记录<br>- 实时推送持仓变化<br>- 实时推送资金变化 | 数据推送延迟<100ms |
| 4.4.3 实现定时数据同步 | 开发组 | 1天 | 在Web应用中实现定时数据同步机制 | 数据更新频率可配置 |
| 4.4.4 优化前端渲染性能 | 开发组 | 1天 | 使用虚拟列表、懒加载等技术优化前端渲染 | 页面加载时间<2s |

### 4.5 阶段五：Web应用与交易引擎连接

**目标**：将Web应用与实际的模拟交易引擎连接

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.5.1 实现Web应用数据获取层 | 开发组 | 2天 | 创建数据获取层，负责：<br>- 从交易引擎获取数据<br>- 数据格式转换<br>- 数据缓存 | 数据获取稳定，支持自动重试 |
| 4.5.2 实现策略管理界面 | 开发组 | 2天 | 创建策略管理界面，支持：<br>- 策略启动/停止<br>- 策略参数配置<br>- 策略状态监控 | 能通过Web界面管理策略 |
| 4.5.3 实现实时监控面板 | 开发组 | 3天 | 升级现有监控面板，支持：<br>- 实时更新图表<br>- 实时推送交易信号<br>- 实时展示成交记录<br>- 实时展示持仓变化 | 监控面板数据实时更新，无明显延迟 |
| 4.5.4 实现模拟交易操作界面 | 开发组 | 2天 | 创建模拟交易操作界面，支持：<br>- 手动下单<br>- 订单查询<br>- 平仓操作<br>- 撤单操作 | 能通过Web界面进行模拟交易 |

### 4.6 阶段六：测试与优化

**目标**：确保系统稳定运行，性能良好

| 任务 | 负责人 | 时间 | 详细描述 | 验收标准 |
|------|--------|------|----------|----------|
| 4.6.1 单元测试 | 开发组 | 2天 | 编写单元测试，覆盖：<br>- 数据持久化模块<br>- 状态恢复模块<br>- 后台服务模块<br>- API服务模块 | 单元测试覆盖率>80% |
| 4.6.2 集成测试 | 开发组 | 2天 | 进行集成测试，验证：<br>- 各模块之间的协作<br>- 数据流转正确性<br>- 系统稳定性 | 集成测试通过率>95% |
| 4.6.3 性能测试 | 开发组 | 1天 | 进行性能测试，验证：<br>- 系统响应时间<br>- 并发处理能力<br>- 资源占用情况 | 系统能支持10个策略同时运行 |
| 4.6.4 压力测试 | 开发组 | 1天 | 进行压力测试，验证：<br>- 系统在高负载下的稳定性<br>- 错误处理机制<br>- 恢复能力 | 系统能承受1000笔/秒的订单量 |
| 4.6.5 优化与修复 | 开发组 | 2天 | 根据测试结果进行优化和修复 | 所有测试用例通过 |

## 5. 技术栈

| 类别 | 技术/框架 | 版本 | 用途 |
|------|----------|------|------|
| 后端框架 | FastAPI | 最新 | REST API服务 |
| WebSocket | FastAPI + WebSocket | 最新 | 实时数据推送 |
| 数据库 | SQLite | 最新 | 数据持久化 |
| 缓存 | Redis | 最新 | 数据缓存 |
| ORM | SQLAlchemy | 最新 | 数据库操作 |
| 后台服务 | Python-daemon | 最新 | 后台进程管理 |
| 进程管理 | Supervisor | 最新 | 服务监控和自动重启 |
| 任务调度 | APScheduler | 最新 | 定时任务执行 |
| 日志管理 | Loguru | 最新 | 日志记录和管理 |
| 前端框架 | Streamlit | 最新 | Web监控面板 |
| 数据可视化 | Plotly | 最新 | 图表展示 |
| 数据处理 | Pandas | 最新 | 数据处理和分析 |

## 6. 关键模块设计

### 6.1 数据模型设计

```sql
-- 订单表
CREATE TABLE orders (
    id TEXT PRIMARY KEY,
    symbol TEXT NOT NULL,
    order_type TEXT NOT NULL,
    direction TEXT NOT NULL,
    quantity REAL NOT NULL,
    price REAL,
    status TEXT NOT NULL,
    create_time DATETIME NOT NULL,
    submit_time DATETIME,
    fill_time DATETIME,
    cancel_time DATETIME,
    reject_time DATETIME,
    strategy_name TEXT,
    account_id TEXT
);

-- 成交记录表
CREATE TABLE fills (
    id TEXT PRIMARY KEY,
    order_id TEXT NOT NULL,
    symbol TEXT NOT NULL,
    direction TEXT NOT NULL,
    fill_quantity REAL NOT NULL,
    fill_price REAL NOT NULL,
    commission REAL NOT NULL,
    fill_time DATETIME NOT NULL,
    strategy_name TEXT,
    account_id TEXT,
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

-- 持仓表
CREATE TABLE positions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,
    quantity REAL NOT NULL,
    avg_price REAL NOT NULL,
    current_price REAL NOT NULL,
    market_value REAL NOT NULL,
    unrealized_pnl REAL NOT NULL,
    realized_pnl REAL NOT NULL,
    update_time DATETIME NOT NULL,
    account_id TEXT NOT NULL,
    UNIQUE(symbol, account_id)
);

-- 投资组合表
CREATE TABLE portfolio (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id TEXT NOT NULL,
    initial_capital REAL NOT NULL,
    current_capital REAL NOT NULL,
    cash REAL NOT NULL,
    total_value REAL NOT NULL,
    update_time DATETIME NOT NULL,
    UNIQUE(account_id)
);

-- 交易记录表
CREATE TABLE trades (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp DATETIME NOT NULL,
    symbol TEXT NOT NULL,
    action TEXT NOT NULL,
    quantity REAL NOT NULL,
    price REAL NOT NULL,
    trade_value REAL NOT NULL,
    fees REAL NOT NULL,
    cash_after REAL NOT NULL,
    account_id TEXT NOT NULL
);

-- 系统状态表
CREATE TABLE system_status (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT NOT NULL UNIQUE,
    value TEXT NOT NULL,
    update_time DATETIME NOT NULL
);
```

### 6.2 后台服务设计

```python
# 后台服务主入口
class BackendService:
    def __init__(self):
        self.event_engine = event_engine
        self.execution_engine = None
        self.portfolio = None
        self.state_manager = StateManager()
        self.transaction_manager = TransactionManager()
        self.scheduler = BackgroundScheduler()
        self.api_server = None
        
    def start(self):
        # 1. 恢复系统状态
        self._restore_state()
        # 2. 初始化执行引擎
        self._init_execution_engine()
        # 3. 启动事件引擎
        self.event_engine.start()
        # 4. 启动定时任务
        self._start_scheduler()
        # 5. 启动API服务
        self._start_api_server()
        # 6. 启动WebSocket服务
        self._start_websocket_server()
        
    def stop(self):
        # 1. 停止API服务
        self._stop_api_server()
        # 2. 停止WebSocket服务
        self._stop_websocket_server()
        # 3. 停止定时任务
        self._stop_scheduler()
        # 4. 停止事件引擎
        self.event_engine.stop()
        # 5. 保存系统状态
        self._save_state()
        
    def _restore_state(self):
        # 从数据库恢复系统状态
        self.state_manager.restore()
        
    def _save_state(self):
        # 保存系统状态到数据库
        self.state_manager.save()
```

### 6.3 WebSocket服务设计

```python
# WebSocket服务实现
class WebSocketManager:
    def __init__(self):
        self.active_connections = []
        
    async def connect(self, websocket: WebSocket):
        await websocket.accept()
        self.active_connections.append(websocket)
        
    def disconnect(self, websocket: WebSocket):
        self.active_connections.remove(websocket)
        
    async def broadcast(self, message: dict):
        for connection in self.active_connections:
            await connection.send_json(message)
        
    def register_event_handlers(self):
        # 注册事件处理器，实时推送数据
        event_engine.register_handler(EventType.FILL, self._on_fill)
        event_engine.register_handler(EventType.ORDER, self._on_order)
        event_engine.register_handler(EventType.SIGNAL, self._on_signal)
        
    async def _on_fill(self, event: FillEvent):
        # 推送成交事件
        await self.broadcast({
            "type": "fill",
            "data": event.data
        })
        
    async def _on_order(self, event: OrderEvent):
        # 推送订单事件
        await self.broadcast({
            "type": "order",
            "data": event.data
        })
        
    async def _on_signal(self, event: SignalEvent):
        # 推送信号事件
        await self.broadcast({
            "type": "signal",
            "data": event.data
        })
```

## 7. 开发里程碑

| 里程碑 | 完成时间 | 交付物 |
|--------|----------|--------|
| M7-1 | 第5天 | 数据持久化模块完成 |
| M7-2 | 第8天 | 重启恢复机制完成 |
| M7-3 | 第11天 | 后台服务完成 |
| M7-4 | 第14天 | 实时数据更新完成 |
| M7-5 | 第18天 | Web应用与交易引擎连接完成 |
| M7-6 | 第21天 | 系统测试与优化完成 |
| M7-7 | 第22天 | 系统上线运行 |

## 8. 风险评估与应对措施

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 数据库性能问题 | 系统响应缓慢 | 1. 使用索引优化查询<br>2. 实现数据分区<br>3. 定期清理历史数据 |
| 后台服务崩溃 | 系统停止运行 | 1. 使用Supervisor自动重启<br>2. 实现健康检查<br>3. 完善错误处理机制 |
| WebSocket连接不稳定 | 实时数据推送延迟或丢失 | 1. 实现重连机制<br>2. 添加心跳检测<br>3. 实现消息确认机制 |
| 数据一致性问题 | 数据不准确 | 1. 使用事务保证数据一致性<br>2. 实现数据校验<br>3. 定期数据同步 |
| 系统资源占用过高 | 系统性能下降 | 1. 优化代码逻辑<br>2. 实现资源监控<br>3. 动态调整资源分配 |

## 9. 质量保证

1. **代码规范**：严格遵守PEP8代码规范，使用Black进行代码格式化
2. **测试覆盖率**：单元测试覆盖率>80%，集成测试覆盖率>95%
3. **文档完善**：每个模块提供详细的API文档和使用说明
4. **代码审查**：所有代码变更必须经过代码审查
5. **性能监控**：实现系统性能监控，包括CPU、内存、磁盘和网络使用情况
6. **日志管理**：完善日志记录，包括错误日志、警告日志和信息日志

## 10. 部署方案

1. **开发环境**：使用Docker容器化部署，便于开发和测试
2. **测试环境**：独立的测试服务器，模拟生产环境配置
3. **生产环境**：
   - 使用Linux服务器部署
   - 后台服务使用Supervisor管理
   - Web应用使用Nginx反向代理
   - 数据库使用SQLite（后期可扩展为MySQL/PostgreSQL）
   - 实现定期备份机制

## 11. 后续优化方向

1. **支持多账户管理**：实现多账户模拟交易
2. **支持多策略并行运行**：支持多个策略同时运行
3. **实盘接口适配**：预留实盘接口，便于后续接入实盘交易
4. **高级回测功能**：实现基于历史数据的高级回测功能
5. **策略自动优化**：实现策略参数自动优化功能
6. **风险预警机制**：实现风险预警和自动止损功能

## 12. 总结

本开发计划详细描述了M7阶段的开发任务、技术架构、关键模块设计和里程碑安排。通过本计划的实施，将实现一个功能完整、性能稳定的实时模拟盘系统，为后续实盘交易奠定基础。

---

**制定日期**：2025-12-18
**负责人**：开发组
**审核人**：项目负责人